import { bundle } from "../../base/util";
import { DisplayConf } from "../display";

const conf: bundle<DisplayConf> = {
	panel: {
		type: "display",
		header: "caption",
		body: "display"
	}
}

export default conf;