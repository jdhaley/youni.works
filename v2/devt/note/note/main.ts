// import {Frame} from "../../items/baseDisplay.js";
// import conf from "./conf.js";

// let frame = new Frame(window, conf.frame);
// let note = frame.create(conf.note);
// frame.append(note);

// let file = frame.getFileName("/journal", "note");
// frame.origin.open(note, file)