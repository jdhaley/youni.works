export default `
;
/* ↖ ↗ ↘ ↙
 * Testing of source.
 */
// Record testing...
a: b : c d : e, c: + 1,, d;
package: "youniworks.com/test";
package parser: "youniworks.com/parser";;
public: {
	Person: pkg.path.Person x.Util.lower {
		name: String,
		age: Number,
		likes: Sequences
	},
	test: Person {
		name: "John Haley",
		age: +32.3e+4,
		likes: [,"food",, "air",, "water", {x + "code" + 32.4},,]
	},
	get location: (,,this,,35.9,),
	valid: (x) {
		out: "";
		f: () {
			return something;
		};
		/* REMOVED STATEMENT SUPPORT
		if (x > 3) {
			out = "four";
		} else if (x < -2) {
			out = "x";
			;
		}
		for (let ele of value) out += a.b(w).compileValue(ele) + " ";
		************************/
		return out;
	},
	casetest: (x, y) {
		switch (expr) {
		  # "a":
		  # "b": ]
			vari: sfdasdfa;
			vari += 30;
		  # "c":
			return 30 + 80;
	 	} else {
			fasdfasf;
		}
	},
	asmtest: (x) {
		/*
		| text: "asdfasdfasdfasdfdasdf"
		. start:
			mov ax, bx;
			jump start;
		*/
	}
};

private: () {
	/* loading code & private variables. */
}
`;
